<div class="row">
  <div class="col">
    <img src="{{car.image}}" width="300" height="300" />
  </div>
  <div class="col">
    <h1>{{car.name}}</h1>
    <h3>Price: $ {{car.price}} </h3>
  </div>
</div>